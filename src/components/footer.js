import React from 'react';

const Footer = () => {
    return (
        <footer className="footer">
            © {new Date().getFullYear()} Oscar Ceballos
        </footer>
    );
};

export default Footer;